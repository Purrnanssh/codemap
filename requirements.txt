# Core backend dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Graph and CLI dependencies
networkx>=3.2
typer>=0.12
rich>=13.7

# Install the current project (src/codemap) to make it importable
.
