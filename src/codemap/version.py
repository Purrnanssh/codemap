"""Version metadata for CodeMap.

We define the version in exactly ONE place, this file and import it
everywhere else. Single source of truth: change it here, and every part of
the project (CLI `--version`, README badges, package metadata) picks it up.
"""

__version__ = "0.4.0"
